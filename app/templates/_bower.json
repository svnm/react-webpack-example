{
  "name": "<%= appname %>",
  "private":true,
  "version": "0.0.1",
  "dependencies": {
    "modernizr": "~2.8.1", <% if (cssFramework === 'SASS') { %>
    "bootstrap-sass-official": "~3.3.1", <% } if (cssFramework === 'LESS') { %>
    "bootstrap": "*", <% } if (moduleLoader === 'requirejs') { %>
    "requirejs": "*", <% } %>
    "jquery": "~2.1.1"
  }
}